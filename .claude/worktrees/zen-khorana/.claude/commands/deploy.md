---
description: Build, verify, and deploy to Vercel
allowed-tools: Bash
---

1. Run `npm run build` — if it fails, stop and report errors
2. Run `npm run lint` — if it fails, stop and report errors
3. Run `vercel --prod` to deploy
4. Report the deployment URL when done
